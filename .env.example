# Authentication - generate a strong random string for production
# e.g. openssl rand -base64 32
AUTH_SECRET=change-me-to-a-strong-random-string

# Daily virtual currency allowance per user
DAILY_ALLOWANCE=1000

# Database path (defaults to ./funmarket.db for local dev)
# DATABASE_URL=file:./funmarket.db
